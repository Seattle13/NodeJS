<footer>
  <section>
    <% if (games.length === 0) { %>
    <p>No games found.</p>
    <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Team 1</th>
          <th>Score 1</th>
          <th>Team 2</th>
          <th>Score 2</th>
        </tr>
      </thead>
      <tbody>
        <% games.forEach(game => { %>
        <tr>
          <td><%= game.date %></td>
          <td><%= teams.find(team => team.id === game.team1Id).name %></td>
          <td><%= game.team1Score %></td>
          <td><%= teams.find(team => team.id === game.team2Id).name %></td>
          <td><%= game.team2Score %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </section>
</footer>
